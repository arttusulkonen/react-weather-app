import React from 'react';import './weather-data.css'import Spinner from '../spinner/'const WeatherData = ({city, country, temp, icon, temp_max, temp_min, descr, error, loading}) => {    if (loading) {        return <Spinner/>    }    return (        <div className='weather'>            {/*if city == true, show weather */}            {city &&            <div>                <p>{city}, {country}</p>                <p className='temp'>{temp}&#8451;</p>                <p><img className='icon' src={`http://openweathermap.org/img/w/${icon}.png`}/></p>                <p>Max temp: {temp_max}&#8451;</p>                <p>Min temp: {temp_min}&#8451;</p>                <p>{descr}</p>            </div>            }            {/*if error == true, show error message*/}            {                error && <p>{error}</p>            }        </div>    )}export default WeatherData;